import { CSSProperties } from 'react';

const cardStyles: CSSProperties = {
  maxWidth: 580,
  width: '100%',
  borderRadius: 16,
  boxShadow: '0 8px 24px rgba(0,0,0,.08)',
  paddingInline: '30px',
};

const flexWrapperStyles: CSSProperties = {
  width: '100%',
  margin: '0 auto',
};

export { cardStyles, flexWrapperStyles };
